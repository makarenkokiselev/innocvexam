server: 
  servlet:
    context-path: /user-api

spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
  jackson: 
    date-format: yyyy-MM-dd
  jpa:
    show-sql: true